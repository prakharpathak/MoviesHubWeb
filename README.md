When you clone the project, remember to change the DB server name accordingly and run DB migrations with package manager console in nuget package manager.

Commands to run -
1. add-migration migration_name
2. update-database
